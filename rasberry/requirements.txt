# 라즈베리파이 카메라 클라이언트 의존성 패키지
# Python 3.9+ 필요

# 웹소켓 통신 (Spring Boot 연결용)
websockets==11.0.3

# OpenCV 영상처리 
opencv-python==4.8.1.78
numpy==1.24.3

# 유틸리티
python-dotenv==1.0.0

# HTTP 요청 (Spring Boot API 호출용)
requests==2.31.0

# Picamera2는 시스템 패키지로 설치 필요
# pip로 설치하지 말고 아래 명령어로 설치:
# sudo apt install python3-picamera2

# Bookworm OS용 시스템 패키지들
# sudo apt update
# sudo apt install -y rpicam-apps
# sudo apt install -y python3-picamera2
# sudo apt install -y python3-opencv  
# sudo apt install -y python3-numpy
# sudo apt install -y python3-websockets